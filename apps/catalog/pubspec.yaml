name: jelly_catalog
description: Interactive development catalog for Jelly UI and first-party morphs.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  jelly_morph_neobrutalism: ^1.0.0-dev.1
  jelly_ui: ^1.0.0-dev.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
